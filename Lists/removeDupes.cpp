// remove duplicates
int main()
{
    // 1) put items of list to a map
    // 2) for every element of the list, check it is not in the map
    //      a) if it is not in the map then continue
    //      b) otherwise remove it
    //      * Note I have to add a remove element function in the list

    // return kth to the last
    // iterate k-1 elements from the tail node

    // delete middle node given the node
    // for n-1, iterate
    // remove node

    // partition.cpp
    // x is user input
    // assume all items are numbers
    // List1 and List2
    // iterate over list
    //  a) if item is <, append to List1
    //  b) ow append item to List2
    
    //
}
